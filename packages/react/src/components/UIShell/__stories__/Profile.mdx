import { ArgTypes, Canvas, Meta } from '@storybook/blocks';
import * as ProfileStories from './Profile.stories';

<Meta isTemplate />

# Profile

## Table of Contents

- [Overview](#overview)
- [Examples](#examples)
  - [Profile](#prpfile)
  - [Profile.UserInfo](#profileuserinfo)
  - [With ThemeSwitcher](#with-themeswitcher)
  - [Profile.ReadOnly](#profilereadonly)
  - [With links](#with-links)
- [Component API](#component-api)

{/* <!-- END doctoc generated TOC please keep comment here to allow auto update --> */}

## Overview

The `Profile` is a large family of compoments, composed of multiple zones; the
UserInfo, ThemeSwitcher, ReadOnlyInfo, and Links.

<Canvas of={ProfileStories.Default} />

## Profile.Root

The `Profile.Root` component is used to handle the composed components.

```javascript
<Profile.Root label="Profile" renderIcon={<UserAvatar size={20} />}>
  <Profile.UserInfo name="Thomas J. Watson" email="thomas.watson@ibm.com" />
</Profile.Root>
```

## Profile.UserInfo

The `Profile.UserInfo` goes inside the `Profile.Root` and requires a `name` prop
and an optional `email` prop. It extends IBM Product’s
[UserAvatar](https://ibm-products.carbondesignsystem.com/?path=/docs/components-useravatar--docs&globals=viewport:basic),
and supports all `UserAvatar` props. Here’s an example of how it can be used
within `<Profile.Root>`:

```javascript
<Profile.Root label="Profile" renderIcon={<UserAvatar size={20} />}>
  <Profile.UserInfo name="Thomas J. Watson" email="thomas.watson@ibm.com" />
</Profile.Root>
```

## With ThemeSwitcher

In order to add the ThemeSwitcher to the Profile one can utilize the
`ThemeSettings` and `ThemeSwitcher` components from
`@carbon-labs/react-theme-settings`. More info on how to use the ThemeSwitcher
can be found
[here](https://labs.carbondesignsystem.com/?path=/docs/react_components-themesettings--overview).

<Canvas of={ProfileStories.WithThemeSwitcher} />

## Profile.ReadOnly

The `Profile.ReadOnly` goes inside the `Profile.Root` and accepts a list of
items that contain objects with a label and title.

<Canvas of={ProfileStories.withReadOnly} />

## With links

In order to add links to the Profile one can utilize the `ContainedList` and
`ContainedListItem` components from `@carbon/react`.

<Canvas of={ProfileStories.withLinks} />

## Component API

<ArgTypes />
````
