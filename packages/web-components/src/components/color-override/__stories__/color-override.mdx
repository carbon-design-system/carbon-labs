import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as ColorOverrideStories from './color-override.stories';

<Meta of={ColorOverrideStories} />

# Color Override

An SCSS-only style package that provides theme-aware color overrides using
Carbon Design System tokens for tags.
