/**
 * @license
 *
 * Copyright IBM Corp. 2025
 *
 * This source code is licensed under the Apache-2.0 license found in the
 * LICENSE file in the root directory of this source tree.
 */

import { html, fixture, expect } from '@open-wc/testing';
import '@carbon-labs/wc-ui-shell/es/index.js';

describe('clabs-ui-shell', function () {
  it('should render with cds-button minimum attributes', async () => {
    const el = await fixture(html`<clabs-ui-shell> button </clabs-ui-shell>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header', function () {
  it('should render with clabs-header minimum attributes', async () => {
    const el = await fixture(html`<clabs-header></clabs-header>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-global-action', function () {
  it('should render with clabs-header-global-action minimum attributes', async () => {
    const el = await fixture(
      html`<clabs-header-global-action></clabs-header-global-action>`
    );

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-menu', function () {
  it('should render with clabs-header-menu minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-menu></clabs-header-menu>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-menu-button', function () {
  it('should render with clabs-header-menu-button minimum attributes', async () => {
    const el = await fixture(
      html`<clabs-header-menu-button></clabs-header-menu-button>`
    );

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-menu-item', function () {
  it('should render with clabs-header-menu-item minimum attributes', async () => {
    const el = await fixture(
      html`<clabs-header-menu-item></clabs-header-menu-item>`
    );

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});

describe('clabs-header-name', function () {
  it('should render with clabs-header-name minimum attributes', async () => {
    const el = await fixture(html`<clabs-header-name></clabs-header-name>`);

    await expect(el).dom.to.equalSnapshot();
    await expect(el).shadowDom.to.be.accessible();
  });
});
